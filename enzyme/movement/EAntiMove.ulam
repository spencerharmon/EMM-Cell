use Once;
element EAntiMove : QEnzyme {
  Once o;
  @Override Void specialEnzymeBehavior(){
    if(o.new()){
      ++self.age.time;
    }
    for(SiteNum i = 1; i < ew.size(); ++i){
      Atom a = ew[i];
      if(a as QEMove){
        ew[i] = self;
      }
    }
  }
}