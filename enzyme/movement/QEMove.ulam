/**
  
 */
quark QEMove : QEnzyme {
  virtual Void specialQEMoveBehavior();
  @Override Void specialEnzymeBehavior(){
    specialQEMoveBehavior();
    for(SiteNum i = 1; i < ew.size(); ++i){
      Atom a = ew[i];
      if(a as ESymmetry){
        ew[0] = a;
      }
    }
  }
}