use GID;
element IDCopy : QGeneCopy{
  typedef MetaID.ID ID;
  ID id;
  GCType mGCType;

  @Override GCType & getGCType(){
    return mGCType;
  }

  @Override Bool complete(){
    return true;
  }
  @Override Atom convert(){
    GID aId;
    aId.id = id;
    return aId.atomof;
  }
}